function solve(input) {
    for (let h = 0; h < 24; h++) {
        for (let m = 0; m < 60; m++) {
            console.log(h + " : " + m)
        }
    }
}

solve()

/*for (let im = i; im < i + 1; im++) {
    let num = Number(input.shift());
    let num2 = Number(input.shift());
    sum2 = num + num2;
}
for (let ima = im; ima < im + 1; ima++) {
    let num = Number(input.shift());
    let num2 = Number(input.shift());
    sum3 = num + num2;
}*/