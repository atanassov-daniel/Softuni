function solve(input) {
    let change = Number(input.shift());
    let coinsCount = 0;
    while (change>0) {
        

        coinsCount++
    }
    console.log(total)
}

solve([
    "1.23",
    "2",
    "0.56",
    "2.73",
])
